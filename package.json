{
  "name": "cardshop-serra-shopify-feed",
  "version": "1.0.0",
  "description": "Wisdom Guild feed generator for Shopify",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node --env-file=.env scripts/generate-feed.js"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}
